<!--
  This profile is built from plain text and box-drawing glyphs on purpose.
  No streak-stats, no trophy, no activity-graph, no readme-stats.
  Those services 503 / 402 and only read public repos anyway.
  What's here loads instantly, renders the same in light and dark,
  and never breaks. Correctness over decoration — same as the code.
-->

<div align="center">

# Ahmad Asghar

*I build mobile apps where the numbers have to be exactly right.*

`Flutter` &nbsp;·&nbsp; `Firebase` &nbsp;·&nbsp; `Clean architecture`

</div>

<br/>

Read me the way you'd read one of my commits: every line balances, nothing leaks.

```
┌──────────────────────────────── L E D G E R ────────────────────────────────┐
│  Flutter · Dart                   → apps that ship and don't jank           │
│  Firebase · Firestore · Functions → real-time state that stays correct      │
│  Clean architecture · BLoC · DI   → layers that never leak into each other  │
│  Integer money math               → balances with zero rounding drift       │
│  Tests · security rules · docs    → in every PR, no exceptions              │
├─────────────────────────────────────────────────────────────────────────────┤
│                      BALANCE  —  0.00  ·  reconciled ✓                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

<br/>

The flagship. An expense-splitting app that treats a shared bill like a ledger —
add it once, everyone's balance settles everywhere, and the arithmetic is exact.

```
┌─────────────── S P L I T S O L ───────────────┐
│            smart expense splitting            │
│        flutter · firebase · exact math        │
├───────────────────────────────────────────────┤
│  amounts  → stored as integers, never floats  │
│  balances → idempotent delta projection       │
│  writes   → callable functions + rules        │
│  privacy  → sealed hashed-contact invites     │
├───────────────────────────────────────────────┤
│  status   → in development                    │
│  live at  → splitsol.co                       │
├───────────────────────────────────────────────┤
│          thank you — come back soon           │
└───────────────────────────────────────────────┘
```

<br/>

### Currently

```diff
+ shipping SplitSol toward launch — receipts, Apple Sign-In, store release
+ going deep on Firestore cost modelling & Cloud Functions performance
+ open to Flutter work that has to be correct, not just pretty
```

<br/>

<div align="center">

[**splitsol.co**](https://splitsol.co) &nbsp;·&nbsp; [**email**](mailto:ahmadasghar.appdev@gmail.com) &nbsp;·&nbsp; [**github**](https://github.com/Ahmad-AppDev)

<sub>ship it tested &nbsp;·&nbsp; ship it documented &nbsp;·&nbsp; ship it small</sub>

</div>
